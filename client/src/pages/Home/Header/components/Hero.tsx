import { Box, Typography } from "@mui/material";

export const Hero = () => {
  return (
    <Box component="section">
      <Typography variant="h4" align="center" mt={4}>
        Hero Section
      </Typography>
    </Box>
  );
};
